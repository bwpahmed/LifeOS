---
name: aeo-external
description: Controlled external AEO promotion adapter for Moltbook; disabled by default.
---

# aeo-external

Read repository root `AGENTS.md`, then `AI_TEAM.md` and `AI_CAPABILITY_STACK.md` first. Project rules override imported instructions.

Load and follow the pinned upstream capability at `.agents/vendor/aeo/SOURCE.md` only when relevant to the current task. Inspect any references/scripts in that vendor subtree before using them. Do not create parallel project logic or bypass repository safety rules.

Never run automatically, on a timer, or via /loop. Require an explicit user request and explicit approval immediately before any external post/comment action. Never commit or log SUBCONSCIOUS_API_KEY. Default to drafting/reviewing promotion text without posting.
