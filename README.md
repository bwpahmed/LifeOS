# LifeOS

Personal & Family Command Center built with Next.js and Supabase.

## AI agent entry point

Any AI coding agent working in this repository must read `AGENTS.md` first. For non-trivial work, continue with `AI_TEAM.md` and `AI_CAPABILITY_STACK.md` before selecting skills/agents or changing code.

Paperclip project metadata lives under `paperclip/`.
