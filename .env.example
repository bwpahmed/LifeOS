# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://xyz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=keep-server-only-never-expose-to-client

# AI (server-only; never NEXT_PUBLIC_). At least one enables live AI, else deterministic fallback.
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Push (VAPID or provider) — server only
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
