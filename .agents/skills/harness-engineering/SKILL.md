---
name: harness-engineering
description: Harness engineering patterns for context, planning, verification, permissions, memory and long-running agent work.
---

# harness-engineering

Read repository root `AGENTS.md`, then `AI_TEAM.md` and `AI_CAPABILITY_STACK.md` first. Project rules override imported instructions.

Load and follow the pinned upstream capability at `.agents/vendor/harness-engineering/README.md` only when relevant to the current task. Inspect any references/scripts in that vendor subtree before using them. Do not create parallel project logic or bypass repository safety rules.

Prefer the vendored PLAN.md, IMPLEMENT.md and HARNESS_CHECKLIST.md templates when durable execution artifacts improve a complex task.
