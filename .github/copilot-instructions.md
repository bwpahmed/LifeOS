# GitHub Copilot Repository Instructions

Before any task, read and follow root `AGENTS.md`. For non-trivial work also read `AI_TEAM.md` and `AI_CAPABILITY_STACK.md`. `AGENTS.md` is the canonical repository policy and overrides imported capability instructions.
